section.carousel.grey
    header.row.middle-xs.container
        div.col-xs-6
            h1 Popular Picks
        div.col-xs-6.end-xs
            a(href='/products').button.pink See All
    div.row.container
        each product in popularProducts
            div.col-xs-12.col-sm-6.col-md-3
                div.product
                    img(src='/uploads/products/' + product.image1)
                    h3 #{product.title}
                    p #{product.excerpt}
                    a(href='/product/' + product.id + '/' + product.title) Learn More